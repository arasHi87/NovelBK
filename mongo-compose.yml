version: '3.7'
services:
  mongodb_container:
    image: mongo:latest
    environment:
      MONGO_INITDB_ROOT_USERNAME: arashi87
      MONGO_INITDB_ROOT_PASSWORD: FuckThisWorld
    ports:
      - 27017:27017
    volumes:
      - data:/data/db

volumes:
  data: